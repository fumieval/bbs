module Lib.Types (Text, ByteString, UTCTime, Generic, FromJSON, ToJSON) where

import Data.Aeson
import Data.ByteString (ByteString)
import Data.Text (Text)
import Data.Time
import GHC.Generics (Generic)
